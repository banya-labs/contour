# 07.1 — Edgeless Canvas 1: System Topology & Multi-Surface Flow

> **AFFiNE Mode**: Edgeless Whiteboard Canvas  
> **How to Use**: In AFFiNE, switch to **Edgeless Mode** (Whiteboard) on this page to visualize the interactive multi-surface system topography.

---

## Interactive Topology Diagram (Mermaid)

```mermaid
flowchart TB
    subgraph Edge ["🌐 Public Traffic & Edge Layer"]
        DNS["Cloudflare DNS & SSL"]
        Proxy["Dokploy Traefik Reverse Proxy"]
    end

    subgraph Surfaces ["🖥️ Contour Multi-Surface Architecture"]
        Public["Surface 1: Public Marketing & Listing Cards\n(/ and /p/[slug])"]
        Dashboard["Surface 2: Brokerage Operations Dashboard\n(/dashboard)"]
        FieldPWA["Surface 3: Field Agent Mobile PWA\n(/kiosk & /agent)"]
        Admin["Surface 4: Admin & Mission Control\n(/admin)"]
        Machine["Surface 5: Machine & MCP Server\n(/api/mcp)"]
    end

    subgraph ClientSites ["🏢 Client Integrations"]
        Mals["malsproperty.co.zm\n(WordPress / Divi Widget)"]
    end

    subgraph CoreEngine ["⚙️ Application Runtime & Services"]
        App["Next.js 14 Standalone Container\n(Node 20 Alpine)"]
        BetterAuth["Better Auth Multi-Tenant & RBAC Engine"]
        PowerSync["PowerSync Logical Replication Service"]
        Dify["Dify AI Companion Backend"]
        WhatsApp["Evolution API / Twilio WhatsApp Gateway"]
    end

    subgraph DataTier ["🗄️ Storage & Database Tier"]
        Postgres[("PostgreSQL 16 + pgvector\n(Prisma ORM)")]
        MinIO[("MinIO S3 Encrypted Object Vault\n(Title Deeds & Photos)")]
        Redis[("Redis 7 + BullMQ\n(Queues, Locks & Limiter)")]
        ClientSQLite[("Local SQLite WASM Engine\n(Inside Field Agent Phone)")]
    end

    DNS --> Proxy
    Proxy --> Surfaces
    Mals <--> Public
    Surfaces --> App
    App --> BetterAuth
    App --> Postgres
    App --> MinIO
    App --> Redis
    Postgres --> PowerSync
    PowerSync --> ClientSQLite
    ClientSQLite --> FieldPWA
    WhatsApp <--> Dify
    Dify <--> Machine
```
