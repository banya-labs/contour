import { describe, expect, it } from "vitest";
import { calculateOfferDiscount, isOfferEligible } from "./billing-offers";
const activeOffer = { status: "ACTIVE", kind: "PERCENTAGE" as const, value: 10, startsAt: null, endsAt: null, maxRedemptions: 2, redeemedCount: 0 };
describe("billing offers", () => { it("rejects expired, inactive, and exhausted grants", () => { expect(isOfferEligible({ offer: activeOffer, grant: { status: "ACTIVE", expiresAt: new Date(Date.now() - 1) } })).toBe(false); expect(isOfferEligible({ offer: { ...activeOffer, status: "PAUSED" }, grant: { status: "ACTIVE", expiresAt: null } })).toBe(false); expect(isOfferEligible({ offer: { ...activeOffer, redeemedCount: 2 }, grant: { status: "ACTIVE", expiresAt: null } })).toBe(false); }); it("calculates percentage and fixed discounts without exceeding the subtotal", () => { expect(calculateOfferDiscount({ kind: "PERCENTAGE", value: 10, subtotal: 1000 })).toBe(100); expect(calculateOfferDiscount({ kind: "FIXED_AMOUNT", value: 1500, subtotal: 1000 })).toBe(1000); }); });
