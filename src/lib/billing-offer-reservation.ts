import { db } from "@/lib/db";

export async function reserveOrganizationOffer(input: { organizationId: string; offerId: string; paymentId: string }) { return db.$transaction(async (tx) => { const now = new Date(); const grant = await tx.organizationOffer.findFirst({ where: { organizationId: input.organizationId, offerId: input.offerId, status: "ACTIVE", OR: [{ expiresAt: null }, { expiresAt: { gt: now } }] }, include: { offer: true }, orderBy: { createdAt: "asc" } }); if (!grant || grant.offer.status !== "ACTIVE" || (grant.offer.startsAt && grant.offer.startsAt > now) || (grant.offer.endsAt && grant.offer.endsAt <= now)) return null; const claimed = await tx.organizationOffer.updateMany({ where: { id: grant.id, status: "ACTIVE" }, data: { status: "RESERVED", reservedPaymentId: input.paymentId, reservedAt: now } }); if (claimed.count !== 1) return null; return { grantId: grant.id, offerId: grant.offerId, paymentId: input.paymentId, kind: grant.offer.kind, value: Number(grant.offer.value), currency: grant.offer.currency }; }); }
export async function releaseOrganizationOffer(paymentId: string) { return db.organizationOffer.updateMany({ where: { reservedPaymentId: paymentId, status: "RESERVED" }, data: { status: "ACTIVE", reservedPaymentId: null, reservedAt: null } }); }
export async function commitOrganizationOffer(paymentId: string) { return db.$transaction(async (tx) => { const grant = await tx.organizationOffer.findFirst({ where: { reservedPaymentId: paymentId, status: "RESERVED" }, include: { offer: true } }); if (!grant) return null; const redemption = await tx.platformOffer.updateMany({ where: { id: grant.offerId, status: "ACTIVE", OR: [{ maxRedemptions: null }, { maxRedemptions: { gt: grant.offer.redeemedCount } }] }, data: { redeemedCount: { increment: 1 } } }); if (redemption.count !== 1) return null; await tx.organizationOffer.update({ where: { id: grant.id }, data: { status: "REDEEMED", reservedPaymentId: null, reservedAt: null } }); return { grantId: grant.id, offerId: grant.offerId, paymentId }; }); }
