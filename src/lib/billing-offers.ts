export type OfferKind = "PERCENTAGE" | "FIXED_AMOUNT";
export interface OfferEligibilityInput { offer: { status: string; kind: OfferKind; value: number; startsAt: Date | null; endsAt: Date | null; maxRedemptions: number | null; redeemedCount: number }; grant: { status: string; expiresAt: Date | null }; now?: Date; }
export function isOfferEligible({ offer, grant, now = new Date() }: OfferEligibilityInput): boolean { if (offer.status !== "ACTIVE" || grant.status !== "ACTIVE") return false; if (offer.startsAt && offer.startsAt > now) return false; if (offer.endsAt && offer.endsAt <= now) return false; if (grant.expiresAt && grant.expiresAt <= now) return false; if (offer.maxRedemptions !== null && offer.redeemedCount >= offer.maxRedemptions) return false; if (!Number.isFinite(offer.value) || offer.value <= 0) return false; if (offer.kind === "PERCENTAGE" && offer.value > 100) return false; return true; }
export function calculateOfferDiscount(input: { kind: OfferKind; value: number; subtotal: number }): number { if (!Number.isFinite(input.subtotal) || input.subtotal <= 0 || !Number.isFinite(input.value) || input.value <= 0) return 0; const raw = input.kind === "PERCENTAGE" ? input.subtotal * (input.value / 100) : input.value; return Math.min(input.subtotal, Math.max(0, raw)); }
