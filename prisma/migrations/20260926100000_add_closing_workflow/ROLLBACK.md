# Rollback guidance

This migration is additive. Do not run rollback SQL in production without an approved backup and a separately reviewed migration. If the feature is disabled, leave the tables in place and stop creating new workflows; existing snapshots preserve the agency policy used by each deal.
