"use client";

import { useCallback, useEffect, useState } from "react";
import Link from "next/link";
import { ArrowLeft, Search, ScrollText } from "lucide-react";

type Event = { id: string; organizationId: string; userId: string | null; action: string; entityType: string; entityId: string | null; details: unknown; createdAt: string; organization: { name: string; slug: string } };
type Pagination = { page: number; totalPages: number; total: number };

export default function AdminAuditPage() {
  const [events, setEvents] = useState<Event[]>([]);
  const [query, setQuery] = useState(""); const [organizationId, setOrganizationId] = useState(""); const [userId, setUserId] = useState(""); const [action, setAction] = useState(""); const [from, setFrom] = useState(""); const [to, setTo] = useState("");
  const [page, setPage] = useState(1); const [pagination, setPagination] = useState<Pagination>({ page: 1, totalPages: 1, total: 0 }); const [message, setMessage] = useState("Loading audit events…");
  const load = useCallback(async (requestedPage = page) => {
    const params = new URLSearchParams({ page: String(requestedPage), pageSize: "50" });
    if (query.trim()) params.set("q", query.trim()); if (organizationId.trim()) params.set("organizationId", organizationId.trim()); if (userId.trim()) params.set("userId", userId.trim()); if (action.trim()) params.set("action", action.trim()); if (from) params.set("from", new Date(`${from}T00:00:00`).toISOString()); if (to) params.set("to", new Date(`${to}T23:59:59.999`).toISOString());
    const response = await fetch(`/api/admin/audit?${params}`); const data = await response.json(); setEvents(data.events || []); if (data.pagination) setPagination(data.pagination); setMessage(response.ok ? "" : data.error || "Unable to load audit events.");
  }, [query, organizationId, userId, action, from, to, page]);
  useEffect(() => { void load(page); }, [load, page]);
  function applyFilters() { setPage(1); void load(1); }
  const field = (label: string, value: string, setValue: (value: string) => void, placeholder: string, type = "text") => <label className="text-xs font-bold uppercase tracking-wider">{label}<input type={type} value={value} onChange={(event) => setValue(event.target.value)} placeholder={placeholder} className="mt-2 h-10 w-full border border-editorial-border px-3 text-sm font-normal normal-case outline-none" /></label>;
  return <main className="min-h-screen bg-editorial-bg px-4 py-6 font-geist text-editorial-black sm:px-8"><div className="mx-auto max-w-7xl space-y-7"><Link href="/admin" className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-editorial-muted"><ArrowLeft className="h-4 w-4" /> Control Plane</Link><header className="flex items-end justify-between border-b border-editorial-border pb-5"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-editorial-red">Platform governance // evidence</p><h1 className="mt-2 font-heading text-4xl font-bold uppercase">Audit log</h1><p className="mt-2 text-sm text-editorial-muted">Recent operator and agency activity, bounded for safe review.</p></div><ScrollText className="hidden h-8 w-8 text-editorial-red sm:block" /></header><div className="grid gap-3 border border-editorial-border bg-white p-4 md:grid-cols-3">{field("Search", query, setQuery, "Action, entity or ID")}{field("Agency ID", organizationId, setOrganizationId, "Organization ID")}{field("User ID", userId, setUserId, "Actor user ID")}{field("Action", action, setAction, "e.g. USER_SIGN_IN")}{field("From", from, setFrom, "", "date")}{field("To", to, setTo, "", "date")}<button onClick={applyFilters} className="inline-flex h-10 items-center justify-center gap-2 bg-editorial-black px-5 text-xs font-bold uppercase tracking-wider text-white md:col-span-3 md:justify-self-end"><Search className="h-4 w-4" /> Apply filters</button></div>{message && <p className="text-sm text-editorial-muted">{message}</p>}<section className="overflow-x-auto border border-editorial-border bg-white"><table className="w-full min-w-[900px] text-left"><thead className="border-b border-editorial-border bg-editorial-paper"><tr className="text-[10px] font-bold uppercase tracking-widest text-editorial-muted"><th className="px-5 py-4">When</th><th className="px-5 py-4">Action</th><th className="px-5 py-4">Agency</th><th className="px-5 py-4">Entity</th><th className="px-5 py-4">Operator</th></tr></thead><tbody className="divide-y divide-editorial-border">{events.map((event) => <tr key={event.id}><td className="px-5 py-4 text-xs text-editorial-muted">{new Date(event.createdAt).toLocaleString()}</td><td className="px-5 py-4 font-mono text-xs font-bold">{event.action}</td><td className="px-5 py-4 text-sm">{event.organization.name}<div className="font-mono text-[10px] text-editorial-muted">/{event.organization.slug}</div></td><td className="px-5 py-4 text-xs">{event.entityType}{event.entityId ? <span className="font-mono text-editorial-muted"> · {event.entityId}</span> : null}</td><td className="px-5 py-4 font-mono text-xs text-editorial-muted">{event.userId || "system"}</td></tr>)}</tbody></table>{events.length === 0 && !message && <p className="p-8 text-center text-sm text-editorial-muted">No audit events match this filter.</p>}</section><div className="flex items-center justify-between border border-editorial-border bg-white px-4 py-3 text-xs"><span className="text-editorial-muted">{pagination.total} events · Page {pagination.page} of {pagination.totalPages}</span><div className="flex gap-2"><button disabled={page <= 1} onClick={() => setPage((value) => value - 1)} className="border border-editorial-border px-3 py-2 font-bold uppercase disabled:opacity-40">Previous</button><button disabled={page >= pagination.totalPages} onClick={() => setPage((value) => value + 1)} className="border border-editorial-border px-3 py-2 font-bold uppercase disabled:opacity-40">Next</button></div></div></div></main>;
}
